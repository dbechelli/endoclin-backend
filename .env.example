# Backend - Variáveis de Ambiente
# Copie este arquivo para .env e DEFINA TODAS as variáveis obrigatórias

# ============ SERVIDOR ============
PORT=3000
NODE_ENV=production

# ============ POSTGRESQL ============
DB_HOST=seu-host-postgres
DB_PORT=5432
DB_NAME=sua-database
DB_USER=seu-usuario
DB_PASSWORD=sua-senha-super-segura

# ============ AUTENTICAÇÃO (JWT) - OBRIGATÓRIO ============
# Credenciais do administrador
ADMIN_USERNAME=admin
# Hash da senha do admin
ADMIN_PASSWORD_HASH=gerar aqui uma hash segura para a senha do admin

# Chaves secretas para JWT (fixas, não mudam)
JWT_SECRET=gerar aqui uma chave secreta segura para JWT
JWT_REFRESH_SECRET=gerar aqui uma chave secreta segura para refresh tokens

# Tempo de expiração dos tokens
JWT_EXPIRES_IN=15m
JWT_REFRESH_EXPIRES_IN=7d

