# Backend - Variáveis de Ambiente
# Copie este arquivo para .env e preencha com seus valores reais

# ============ SERVIDOR ============
PORT=3000
NODE_ENV=production

# ============ POSTGRESQL ============
DB_HOST=seu-host-postgres
DB_PORT=5432
DB_NAME=sua-database
DB_USER=seu-usuario
DB_PASSWORD=sua-senha-super-segura

# ============ AUTENTICAÇÃO (JWT) ============
# Credenciais do administrador
ADMIN_USERNAME=admin
# Hash da senha do admin (gere com: node -e "console.log(require('bcryptjs').hashSync('sua-senha', 10))")
ADMIN_PASSWORD_HASH=$2a$10$seu-hash-bcrypt-aqui

# Chaves secretas para JWT (gere com: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))")
JWT_SECRET=sua-chave-secreta-muito-segura-com-32-caracteres
JWT_REFRESH_SECRET=sua-chave-refresh-muito-segura-com-32-caracteres

# Tempo de expiração dos tokens
JWT_EXPIRES_IN=15m
JWT_REFRESH_EXPIRES_IN=7d
