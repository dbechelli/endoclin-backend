# Backend - Variáveis de Ambiente
# Copie este arquivo para .env e DEFINA TODAS as variáveis obrigatórias

# ============ SERVIDOR ============
PORT=3000
NODE_ENV=production

# ============ POSTGRESQL ============
DB_HOST=seu-host-postgres
DB_PORT=5432
DB_NAME=sua-database
DB_USER=seu-usuario
DB_PASSWORD=sua-senha-super-segura

# ============ AUTENTICAÇÃO (JWT) - OBRIGATÓRIO ============
# Credenciais do administrador
ADMIN_USERNAME=admin
# Hash da senha (gere com: node -e "console.log(require('bcryptjs').hashSync('sua-senha', 10))")
ADMIN_PASSWORD_HASH=gerar hash aqui

# Chave secreta para JWT (fixo, não muda)
JWT_SECRET=gerar chave secreta aqui
